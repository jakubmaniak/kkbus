import React from 'react';

const RouteYearReport = React.forwardRef((props, ref) => {
    return (
        <div className="tile" ref={ref}>
            <h2>Raport roczny</h2>
        </div>
    );
})

export default RouteYearReport;

import React from 'react';

const RouteWeekReport = React.useRef((props, ref) => {
    return (
        <div className="tile" ref={ref}>
            <h2>Raport tygodniowy</h2>
        </div>
    );
});

export default RouteWeekReport;
